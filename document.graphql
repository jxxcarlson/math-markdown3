type Document {
   identifier: String!
   title:String!
   content:String!
   author:ID!
   tags:[String!]!
   timeCreated : Int!
   timeUpdated : Int!
   public : Boolean!
   children: [String!]!
}

type Query {
   # Get one document
   document(identifier: String!): Document
   # Get all documents
   allDocuments: [Document!]!
}

